
str_echo:

takes a file descriptor
unpacks from host to network byte ordering and sets the variable len, if len is greater than 0 it allocates the char* buffer for the string
and reads the string into the buffer. Then it echoes the message back and frees the char* buffer.

doit:

main:

